import React from "react";
import { DefaultLayout } from "../common/DefaultLayout";

export const BrowsePage: React.FC = () => {
  return <DefaultLayout pageTitle="Find requests"></DefaultLayout>;
};
